#include <stdio.h>
/**
 * main -A program used to write from 0 to 10
 * Return: 0 (Success)
 */
int main(void)
{
	int n = 0;
	for (n = 0;n < 10;n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
