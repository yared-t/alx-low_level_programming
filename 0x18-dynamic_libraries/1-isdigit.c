#include "main.h"

/**
 * _isdigit - program used to say digit or not
 * @c: used to store a number
 * Return: 1 if digit Return: 0 if not digit
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
